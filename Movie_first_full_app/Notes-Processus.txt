package.json EXIST = > RUN npm instal

INSIDE package.json => script "start" EXIST => RUN npm start


CREATE TABLE:
TERMINAL => inside db folder => sqlite3 db.moviedatabase

CREATE TABLE my_movies (
id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
title VARCHAR (255) NOT NULL,
type VARCHAR (255),
year SMALLINT(4),
img_url VARCHAR (255)
);

--------------

[X] 1=> add event listener on movie add button

[X] 2=> var array with title, type, year, img_url

[X] 2.1=> consol log var check if ok

[] 3=> link front end js to back end js

[] 4=> add movie array in back-end js to movie table in db

[] 5=> add event listener on LOAD MOVIE button

[] 6=> fetch movie table in db with back end js 

[] 7=> link back end to front end (make new page here in brain/bride?)

[] 8=> link load button to open new page with fetch result